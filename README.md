# CheckSingularityContainer

Checks if apt-get installs work on the specified architecture.

RIGHT NOW, ONLY WORKS WITH UBUNTU!

> perl check.pl --recipe_file=ml.sif --os=ubuntu --codename=bionic --arch=ppc64el

